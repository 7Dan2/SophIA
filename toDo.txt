Ces Todo sont numérotés mais ne représentent pas une liste à réaliser dans l'ordre.

1. Ajouter une verification de version du navigateur => Préviens si certaines fonctionnalité ne seront
pas disponible (i.e : les champs date).

2. Dans la fonctionnalité de renommage des fichiers, le bouton d'action sera inactif (grisé ..) 
tant que les champs de génération des noms ne seront pas remplis.

3. Proposer d'envoyer les fichiers modifiés par mail (modal ? / sento() ?).

4. Modal avec les infos sur le programme et (lien 'a propos').

5. Vérification des noms de fichiers en entrée du renommage (doivent être dejà en .pdf).

6. Faire la documentation (accessible en ligne et en pdf)